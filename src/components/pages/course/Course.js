import React from 'react';
import Courses from '../../shareFile/Courses';

const Course = () => {
    return (
        <div>   
            <Courses></Courses>
        </div>
    );
};

export default Course;